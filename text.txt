(1) rm -rf .git/

(2) git init

(3) git remote add origin url from github repositery

(4) git commit -m “Commit message”

(5) git push -f origin master


----------------------------------------------------------------------

Steps to deploy in render

give the name
select the region 
leave the root directory as emply 
build cvommand =====> nom install
run command ========? node sewrver.js
env variable ==> BD_URL
value=====> connection string of mongodb


